[pytest]

python_files =
    sandy/*.py

addopts =
   -s
   -v
   --doctest-modules
   --basetemp=sandy_tests

doctest_optionflags= NORMALIZE_WHITESPACE

filterwarnings =
    ignore::UserWarning
    ignore::DeprecationWarning
